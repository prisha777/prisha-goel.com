
  # prishagoel (Community)

  This is a code bundle for prishagoel (Community). The original project is available at https://www.figma.com/design/nZxxisklJ7nsa1GxtpfnQR/prishagoel--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run build` to start the development server.
  
